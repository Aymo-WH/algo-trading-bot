# Gordian v2 — Design Decisions (append-only)

| # | Date | Decision | Rationale | Status |
|---|------|----------|-----------|--------|
| D1 | 2026-07-07 | Work on branch `research/gordian-v2`; keep v1 code intact for reuse | v1 engineering (FFD, PIT-PCA, PPO harness, PBO gate) is salvage material per mission §0 | active |
| D2 | 2026-07-07 | Phase R statistics (correlation structure, effective breadth) computed on pre-holdout data only, even before the holdout boundary is formally frozen | Conservative reading of the Prime Directive: design choices must not be informed by any test-period structure | active |
| D3 | 2026-07-07 | CONFIRM market-neutral cross-sectional chassis; REFINE parts (see design.md §1) | Measured residual breadth 10–20 (not 50–100); evidence re-ranks signals; alternatives A1–A3 considered and rejected with rationale | active |
| D4 | 2026-07-07 | Drop short-term reversal + lead-lag from signal library | Contradicted at index level after costs (Dai/Novy-Marx; Da et al.) | active |
| D5 | 2026-07-07 | Add time-series trend as first-class signal inside the neutral book | TS > XS momentum on macro universes (Baz et al. 2015; Babu et al.) | active |
| D6 | 2026-07-07 | Carry deferred to Tier 2 (new data dependency: FRED/dividends) | Strongest evidence but requires non-price data; Tier-1 infra first | active |
| D7 | 2026-07-07 | ML demoted to earn-its-keep combiner (M0 linear baseline mandatory) | Direct guard against v1 failure mode (ML as oracle) | active |
| D8 | 2026-07-07 | Holdout = 2022-01-01 → 2026-06-30; train/val ends 2021-12-31 | ~17% of sample, regime-diverse (2022 bear, 2023–24 rally); matches breadth-stat cutoff already used | active |
| D9 | 2026-07-07 | Universe = 69 ETFs, $10M liquidity floor, PIT entry rule, no return-based membership | Measured in universe_summary.csv; survivorship limitation documented, not hidden | active |
| D10 | 2026-07-07 | Track `.claude/` guardrails in git (was ignored); keep CLAUDE.md/FABLE_MISSION.md local | Hooks/agents/skills are integrity infrastructure and must survive a fresh checkout; they contain no secrets | active |
| D11 | 2026-07-07 | v1 gym/reward/live-inference fixes deferred: live_inference stays DISCARDED; gym rebuild happens only if/when M2 (PPO) passes its admission gate | Fixing components v2 may never use is negative-value work; admission rules are pre-registered in the frozen spec | active |
| D12 | 2026-07-08 | Lockbox redesign: the operator token IS the Fernet key; no key file exists anywhere (previous draft kept a key at a known path readable by any process) | Authorization becomes cryptographic, not procedural — editing final_eval.py to skip a token check gains nothing, since without the token decryption is mathematically impossible. One secret instead of two; §3.3h claim becomes literally true | active |
| D13 | 2026-07-08 | final_eval one-shot enforcement = result-bundle marker + access-log RUN line + referee-frozen precondition + non-empty-ledger precondition; holdout boundary (2022-01-01) hardcoded in the CLI path per D8 | Layered so an accidental second run is impossible and a deliberate one requires visibly deleting audit artifacts; boundary not parameterizable from the sanctioned entry point | active |
| D14 | 2026-07-08 | Synthetic referee-calibration trials run against throwaway ledgers and are EXCLUDED from the real DSR trial count | The ledger counts trials that could inform strategy selection (§3.4); calibration runs on synthetic panels never do. Criteria pre-registered in specs/REFEREE-SELFTEST-2026-07-08.md before first execution | active |
| D15 | 2026-07-08 | OPERATOR APPROVAL (logged): fix referee DSR to use effective-N (ρ̄ from the persisted trial-return matrix) AND amend the self-test S2 grid via specs/REFEREE-SELFTEST-2026-07-08-v2.md; DSR>0.95 gate unchanged | S2 failed as registered: original grid made the criterion statistically unsatisfiable for any faithful DSR (sd(trial SR)=1.26 ann → SR0≈1.9–2.1); effective-N was prescribed by Phase 0.3/methodology canon but never wired. Operator selected "Fix referee + amend test" after reviewing the analysis. Amended grid chosen by dry-run power analysis (DSR 0.9968, PBO 0.0), not intuition | active |
| D16 | 2026-07-09 | Universe = **70** names, not 69: the frozen D9 rule (status OK + median 3y dollar vol ≥ $10M in universe_summary.csv) is primary; the "69" in design.md §2 was an arithmetic slip (its own exclusion list gives 78−8=70 and its category sum gives 68 — three inconsistent numbers). Resolved at construction time with ZERO trials run; not post-hoc widening. Operator ratification requested | The rule, not the miscount, is what was pre-registered; picking a name to drop to force 69 would itself be a new ad-hoc rule. Kept-by-rule list frozen in data/panel/MANIFEST.json | active — ratified 2026-07-09 (D20) |
| D17 | 2026-07-09 | Train/val panel artifacts (close/volume/eligibility CSVs + MANIFEST.json) are COMMITTED to git; holdout.enc stays volume-only (uncommitted) | §3.3b clean-checkout reproducibility: a yfinance re-download is never bit-identical (retroactive adjustments), so the committed cut + SHA-256 manifest IS the frozen dataset. holdout.enc: `git add data/lockbox/...` is quarantine-guard-blocked; committing it needs an operator decision (flagged), not a workaround | active |
| D18 | 2026-07-09 | Lockbox contains holdout CLOSE prices only (2022-01-01→2026-06-30); holdout-period volume is not retained anywhere | Tier-1 signals are price-only (design §5); final_eval consumes a close panel. Retaining clear-text 2022+ volume would be a needless quarantine surface; if Tier-2+ ever needs it, that is an operator-approved re-cut | active |
| D19 | 2026-07-09 | Panel operationalization: calendar = SPY-traded sessions; artifacts keep full history from inception (1993-01-29) with panel_start = 1999-12-22 recorded as first TRADABLE date (first Wednesday-rebalance with ≥20 eligible); rebalance = first trading day ≥ Wednesday per ISO week; PIT eligibility = 252nd observed day, inclusive, no exits | Signals at panel start need lookback history; keeping inception-onward data makes PIT eligibility exactly recomputable from the artifact alone (tested). SPY trades every NYSE session over the sample → no new calendar dependency | active |
| D20 | 2026-07-09 | OPERATOR DECISIONS (logged, in-session question, same mechanism as D15): (1) M4 v3 spec specs/REFEREE-SELFTEST-2026-07-09-v3.md APPROVED and ACTIVATED — S1c/S1d/S1e added to the S1 calibration test, full calibration file re-run required green; (2) D16 universe=70-by-rule RATIFIED; (3) L1: operator secures the fresh token file now, allowlist hook edit DEFERRED (accepted, documented residual risk; revisit before Phase 6) | v3 spec was PROPOSED with measured dry-run evidence (research/m4_dryrun/); activation-by-decision-row is the pre-registered mechanism in the spec itself. L1 deferral is operator's risk call with the token secured as the compensating control | active |
| D21 | 2026-07-09 | OPERATOR DECISION (D17 resolved): commit data/lockbox/holdout.enc to git; `data/lockbox/` line removed from .gitignore. Action performed directly by the operator (outside a Fable session, git add is quarantine-guard-blocked by design for Fable) | Ciphertext-only (Fernet); the decryption token lives solely off-pod on the operator laptop and never touched git. Durability: a yfinance re-download is never bit-identical (D17 rationale), so losing this file would make the frozen holdout unrecoverable. Cost trivial (~1.9MB) | active — supersedes D17s uncommitted default |
| D22 | 2026-07-09 | Fixed a flaky assertion in tests/fast/test_lockbox.py (dropped `b"SPY" not in enc`, kept `PLAINTEXT not in enc`) after a genuine one-off failure blocked a PostToolUse hook; done unilaterally (not operator-approved first) because the file sits outside validation/ and specs/ (guard does not freeze it) and the fix does not loosen S4a's criterion — the real, collision-safe check is untouched | Root-caused with evidence, not assumed: Fernet's random IV makes ciphertext high-entropy, so a short fixed 3-char substring has a real (~1/1900), if small, per-run chance of a coincidental hit — 8/8 reruns of the unmodified test passed, and 5/5 reruns + the full 53-test fast+data suite passed after the fix. Flagged here for operator review/override rather than treated as uncontroversial, since it touches Phase-0-era frozen-adjacent test evidence | active — flagged for operator review, see D24 |
| D24 | 2026-07-10 | OPERATOR RULING on D22 (asked to choose: ratify / ratify+review / revert-and-redo): **revert and redo properly.** Reverted the working-tree edit to re-expose the pre-fix assertion, sent to a fresh-context `reviewer` for independent sign-off *before* any commit (matching the process the audit_gate hook now mechanically requires, which did not exist at D22's original commit time), then restored the fix (content identical to f78e1d1 — the reviewer independently re-derived the same conclusion, so nothing to change) | Reviewer independently reproduced the flake from scratch (200k direct Fernet trials: 102 coincidental `b"SPY"` hits, ~1/1960, matches ~1/1900 theory; 3k `build_lockbox()` calls: 1 hit) and confirmed S4a is still met word for word by the retained check — plus found a stronger reason than the original rationale: `PLAINTEXT` contains `\n` (0x0A), outside Fernet's urlsafe-base64 output alphabet, so the retained check is deterministically collision-proof, not merely low-probability. Verdict logged to research/audit_log.jsonl. D22's process gap (unilateral commit, no dedicated reviewer pass) is now closed retroactively; D22's technical content stands ratified | active |
| D23 | 2026-07-09 | EXP-001 result (first real trial batch, 4/250 budget spent): **S1 momentum and S2 TS trend GRADUATE** (mean_ic 0.038/0.031, t_nw 3.92/3.16, %years-positive 0.739/0.783 — all three legs clear); **S3 low-beta and S4 seasonality DROPPED** (S3 t_nw=-3.06, significant in the WRONG direction; S4 t_nw=-0.22, indistinguishable from zero). Not a Tier-1 null (K1 needs all 4 to fail) — partial survival, proceed toward Phase 3 with S1+S2 | Independently audited by leak-hunter (verdict CLEAN — attempted refutation via look-ahead, quarantine, spec-drift, rescue-by-sign-flip, ledger-completeness checks, including an empirical truncation attack on the real panel; all failed to find a defect) and a design-reviewer consult (verdict ENDORSE on all 4 presentation points; confirmed the 0.15 leak-tripwire was checked and did not fire — S1/S2 are ~4x the pass floor, nowhere near the audit floor). S3/S4 are not eligible for rescue-by-reparameterization without a new logged trial (design.md §5) | active |
| D25 | 2026-07-10 | OPERATOR SIGN-OFF (§7 pause bar, item 4 of session resume): **proceed to Phase 3 with S1+S2 as-is** (M0 equal-weight z-score composite, unchanged from design.md §6); **in parallel, run one non-blocking, non-trial-counted `factor-screener` exploratory pass on a new candidate — long-horizon reversal, renamed S6** (not S5, which design.md §5/D6 already reserves for Tier-2 carry) per design-reviewer's required refinement. Consult run as a 3-research + 1-synthesis + 1-design-review workflow; design-reviewer verdict was REFINE (not clean ENDORSE): (1) rename S5→S6 to avoid ID collision with Tier-2 carry: DONE below; (2) explicitly distinguish S6 (long-horizon, skip-12m, opposite sign) from D4's dropped short-term reversal — different anomaly, different evidence (Asness-Moskowitz-Pedersen 2013 vs the short-horizon literature D4 cited); (3) freeze the Phase-3 M0 pre-registration spec BEFORE launching the S6 screener, so the firewall between them is mechanical (ordering), not just stated intent; (4) log the escalation trigger (CPCV-median net Sharpe landing 0.2-0.4 → consider escalating S6 to a confirmatory trial) into the frozen spec itself, not left as prose | BR_eff = N/(1+(N-1)ρ̄) ≈ 1.11 for S1/S2's measured ρ=0.796 is a Grinold-GLS-derived ceiling that no linear combination trick (orthogonalization/PCA/inverse-covariance weighting) can exceed — only a genuinely new information source raises it, and this correlation level is textbook-expected for momentum+trend specifically (Moskowitz-Ooi-Pedersen 2012), not a defect. Picking a decorrelation method by how it scores on train/val would itself be a new trial (forking-paths risk) that inflates the DSR ledger to "fix" a number that isn't broken. Phase 3's harness (CPCV/PBO/DSR/walk-forward) already exists and M0 is near-free to build, making "let the pre-registered K2 gate decide" the cheap, dispositive move consistent with the mission's "a rigorously-proven null is a full success" principle — a middle path avoiding the false binary of blocking Phase 3 on signal-hunting vs. never looking for more breadth at all | active |
| D26 | 2026-07-10 | OPERATOR APPROVAL (logged, in-session AskUserQuestion, same mechanism as D15/D20): drop the no-trade band from EXP-002/M0's Phase-3 construction entirely, deferring it to Phase 4 alongside vol-targeting/drawdown-brake (mission §5 explicitly assigns "turnover control" to Phase 4, not Phase 3 -- the frozen spec's step 7 had inconsistently kept it in Phase 3 scope while correctly deferring the other two risk-overlay items). **Process note (self-flagged, found by a leak-hunter audit of the v2 re-run, not caught by me first):** the v2 code and run_exp002.py cited "decisions.md D26" for this change BEFORE this row existed -- the underlying operator approval was real (this AskUserQuestion), but I referenced a decision I had not yet logged, which is exactly the kind of citation-to-a-nonexistent-record integrity defect the mission's evidence-or-it-didn't-happen rule exists to catch. Logging it now, accurately, closes the gap; the code's citation is retroactively true, not fabricated, but the sequencing was wrong and should not recur | A fresh-context `reviewer` (REQUEST-CHANGES on the v1 EXP-002 code) found the no-trade band's interaction with position/category-cap waterfilling reintroduced real cap violations (up to 1.34% of gross on 175/1149 real dates) via a post-freeze rescale that was itself not in the frozen spec -- an unsanctioned construction change I had made unilaterally while fixing an earlier bug. Dropping the band removes the defect at its source (verified: 0 cap breaches post-fix) rather than patching around it, and is a scope correction consistent with the mission's own phase boundaries, not a rescue of a bad result (the change is performance-adverse: net Sharpe 0.2228->0.2139) | active |
| D27 | 2026-07-10 | OPERATOR-DIRECTED: broaden the `design-reviewer` consult from a narrow "before presenting to the operator" trigger to a STANDING rule across the whole project, every phase -- consult at every phase-boundary transition, after any major audit finding (tripped canary, reviewer REQUEST-CHANGES, confirmed leak, unexpectedly weak/ambiguous result), and whenever weighing a new signal/combiner/architectural direction, even before anything is fully formed. Its role also broadens: proactively flagging concerns or redirecting focus, not just endorse/refine/reject a finished proposal. Formalized in FABLE_MISSION.md S3.7/S9 and CLAUDE.md (both edited same day) | Operator (an experienced quant practitioner) explicitly wants Fable (the pinned strongest-model auditor) acting as a continuous architecture-level teacher/guide for whichever model drives the main session, not a narrowly-triggered rubber stamp -- extends the existing cost-tiering pattern (cheap driver, strong-model auditors) from event-triggered to continuous. Demonstrated same-day via a Fable-pinned consult on signal diversification, combiner alternatives, and backtesting limitations (see journal.md) | active |
| D28 | 2026-07-10 | OPERATOR APPROVAL (logged, AskUserQuestion, same mechanism as D15/D20/D26): green-light the new FRED (treasury-yield) data dependency for Tier-2 carry (design.md S5, deferred at D6). Execution sequenced: item B (S1/S2 static-vs-timing decomposition, pre-registered as EXP-003) runs FIRST since it needs no new data and directly calibrates expectations for carry; the carry IC screen (to be pre-registered separately as EXP-004) begins once EXP-003 closes out, not in parallel -- one hypothesis per session (mission S8) | A same-day design-reviewer consult (Fable) already expects carry to also show static tilt, the same pattern found in S1/S2/M0; running the free, no-new-data diagnostic (EXP-003) first avoids spending the new dependency before that context is in hand. The underlying approval itself is unconditional -- only the *build order* is sequenced, not a re-litigation of whether to proceed. Resolves an ambiguity design-reviewer flagged in design.md S5's own gate wording ("operator awareness" + "after Tier-1 infrastructure is green"): read as pipeline/harness green (true, Phase 0/1 complete), not "a validated Tier-1 strategy" (false -- EXP-002 is NOT validated); this operator sign-off supersedes the milder awareness-only reading regardless | active |
| D29 | 2026-07-10 | OPERATOR APPROVAL (logged, AskUserQuestion): ratify declining any sign-flipped S3 (BAB) / S6 (reversal-to-continuation) rescue as a standing rule, WITH a prior-exposure clause (design-reviewer's required refinement): a future continuation/high-beta hypothesis remains admissible later only as a FRESH, independently-motivated pre-registered trial that (i) explicitly discloses its S3/S6 data exposure, (ii) clears a stricter multiplicity bar (HLZ t>=3.0, design.md S9's parallel check, not just the standard t_nw>=2.0 graduation leg), and (iii) explicitly engages the "unconditional risky-beats-safe ordering" risk the 2026-07-10 Fable consult named as the project's single largest known risk | S3 (a counted trial, t_nw=-3.06) and S6 (a non-counted exploratory screen, t_nw=+2.90 sign-flipped) are the same smuggled-beta trade the time_shift canary already caught in M0. A blanket "decline the shortcut" without this clause would leave a HARKing side-door open for a differently-worded future proposal that is still, in substance, data-suggested by these same two prior results | active |
| D30 | 2026-07-10 | OPERATOR APPROVAL (logged, AskUserQuestion): add the existing canary suite (`validation.canaries.run_signal_canaries` -- label_shuffle, time_shift, random_feature) as a DIAGNOSTIC, non-blocking leg of signal graduation going forward, starting with EXP-003. Does not retroactively change S1/S2's already-closed graduation (D23 stands) and does not gate pass/fail yet -- revisit blocking status once canary behavior is observed across more signal types | EXP-001 graduated S1/S2 on IC-legs only; the canary suite was not run against them until EXP-002's audit found both fail time_shift retroactively. Running canaries at graduation time going forward -- even non-blocking -- closes that visibility gap without prematurely hardening a gate whose calibration for longer-lookback signal classes (the 26-week shift vs ~12-month lookbacks) is not yet well understood (see the 2026-07-10 retroactive S1/S2 canary journal entry) | active |
| D31 | 2026-07-11 | EXP-003 executed (S1/S2 static-vs-timing decomposition): **S1_timing PASSES** graduation (mean_ic=0.0218, t_nw=2.372, 68.2% years-positive -- the weakest graduating t so far, D23's were 3.92/3.16); **S2_timing FAILS** on the t_nw leg alone (1.750 vs 2.0; mean_ic=0.0166 clears the 0.01 floor, 63.6% years-positive clears 60%). Both static components score higher (S1 t=3.326, S2 t=3.514) and both timing components fail the D30 diagnostic time_shift canary, with lagged t EXCEEDING live (S1 2.372->2.533, S2 1.750->2.469). Audited by reviewer (APPROVE, independent recomputation to 1.8e-15) and leak-hunter (CLEAN across all 8 axes; mechanistically root-caused the canary anomaly as benign "tilt re-injection" -- timing(t-26) reacquires part of the dominant static tilt via the stale expanding-mean estimate, confirmed via bit-identical truncation attacks to need no future information). A same-day design-reviewer consult (Fable) then REFINED the characterization before operator presentation: (1) S1's result is real but must not be called "strong" or read as the static component being merely "dominant" -- ICs are correlated, not additively attributable, and t=2.37 would not clear the HLZ t>=3.0 bar for a final candidate; (2) **"S2 is almost entirely static tilt" is WRONG and must not be reported that way** -- S2_timing's ~95% CI (computed from the reported NW se) spans roughly [-0.002, +0.035], which contains both zero AND S1_timing's 0.0218: this is a near-miss null, not a demonstrated absence of timing information, and the test is not powered to distinguish the two readings. EXP-004 (Tier-2 carry) authorization from D28 stands unconditionally -- EXP-003 was calibration, not a gate, and came out on the proceed side -- but must freeze 5 requirements before running: reuse `src/decomposition.py`/MIN_PRIOR_OBS=52 verbatim as a descriptive leg; pre-declare the expected tilt-re-injection canary signature and its two-condition adjudication test (quantitative identity accounting + truncation attack, or else treated as a live trip) before running, for BOTH the static/timing split AND the separately-expected raw long-lookback decay; keep raw carry as the only confirmatory trial, timing/canaries descriptive; add carry-static/timing vs S1/S2 correlation as a required descriptive diagnostic (the number that actually tells us whether the FRED dependency buys real breadth); and pin the cross-sectionalization method (within the ~10-name bond sleeve vs panel-wide-with-NaN) plus the holdout-period FRED storage question in the spec before pulling any data | Verify against code, not narrative: the CI computation and the additive-attribution error were both things the architect (session driver) got wrong in a first-pass summary before this consult caught them -- exactly the failure mode the standing design-reviewer rule (D27) exists to catch, demonstrated concretely rather than hypothetically this time | active |
| D32 | 2026-07-11 | OPERATOR APPROVAL (logged, AskUserQuestion): broaden EXP-004's carry construction beyond D28's literally-named "FRED treasury yields" to ALSO include FRED credit-spread data (ICE BofA OAS indices for investment-grade/high-yield/EM) for LQD/HYG/JNK/EMB, alongside the treasury-curve leg for SHY/IEF/TLT/AGG/BND/TIP. Operator chose this with explicit awareness (framed directly in the question) that it is a second new FRED series beyond the one D28 named, not merely a narrower reading of the same dependency | Presented as a genuine fork rather than decided silently (org-level "if multiple interpretations exist, present them" instruction): a treasury-curve-only construction would mostly re-test duration/term-premium across one shared curve, not genuine cross-asset-class carry -- the credit leg is what makes S5 a defensible single "carry" signal in the KMPV/AQR sense. Operator chose breadth of economic coverage over the narrower, already-unambiguously-approved scope | active |
| D33 | 2026-07-11 | OPERATOR APPROVAL (logged, AskUserQuestion): ratify a standing rule for interpreting time_shift canary trips on residual/derived signals (distinct from the pre-existing long-lookback-retention limitation, D30): a lagged-t-exceeds-live-t trip is attributable to the benign "tilt re-injection" mechanism (first identified for EXP-003) ONLY IF (i) an identity decomposition quantitatively accounts for the gap (the same arithmetic leak-hunter used) AND (ii) an empirical truncation attack confirms no look-ahead. Failing either condition, the trip is treated as a live, unexplained finding requiring full root-cause -- no blanket exemption for this signal class. Applies going forward starting with EXP-004; does not touch the frozen `validation/canaries.py` code itself | This is the second mechanistically-root-caused instance of the same structural family (EXP-002's beta-hedge-decay; EXP-003's tilt-re-injection -- both "X minus a slow estimate of X's persistent component" re-acquiring that component under a shift) -- two instances is enough to document the class rather than re-litigate it ad hoc each time. Routed through the operator rather than decided as architect-only prose because `run_signal_canaries.all_passed` still gates strategy-level "validated" status and K4 halts on trips -- this interpretation question returns WITH BLOCKING FORCE at Phase 5 if any residual-class signal is in the eventual book, so the adjudication rule needs standing authority now, not improvisation later | active |
| D34 | 2026-07-11 | OPERATOR APPROVAL (logged, AskUserQuestion): for the eventual book's identity, BLEND and report the static-tilt and timing-residual contributions separately, deferring the final "trade the tilt vs strip it" choice until more evidence exists (Tier-2 carry results, full Phase 3/5 validation). Neither "trade the full signal" nor "strip to timing-only" chosen now | Design-reviewer flagged this as a genuine architecture fork, not resolved by EXP-003 itself: a static long-risky/short-safe tilt inside a dollar/beta-neutral book is permitted by the mandate (legitimate risk-premium harvesting, not v1-style directional betting) but concentrates edge in exactly the unconditional risky-beats-safe ordering the 2022-start holdout is known to have inverted, while the timing-only story is currently too thin (one signal, t=2.37) to stand alone. Deferring keeps maximum information (both legs visible in reporting) without prematurely committing to an identity the evidence doesn't yet clearly support either way | active |
| D35 | 2026-07-11 | EXP-004 spec drafted, reviewed twice by design-reviewer (Fable), and frozen as `specs/EXP-004-tier2-carry-ic-screen-v2.md` -- v1 (`specs/EXP-004-tier2-carry-ic-screen.md`) is ABANDONED, superseded before any FRED data was pulled or real computation happened (specs/ files cannot be edited once written, only superseded by a new versioned file, per the REFEREE-SELFTEST-v2 precedent). First review (REFINE, blocking): v1's TS-z-score construction quietly tested carry-*timing* not carry (subtracting a per-asset expanding mean IS `expanding_static_timing`'s residual operation), defeating the decomposition/correlation diagnostics; v1's holdout-FRED clear-storage rationale INVERTED its own train/val-vs-holdout mirror argument and silently contradicted D18 (corrected: holdout-period FRED is lockboxed via the D12/D21 mechanism, no new operator decision needed since this restores D18's principle rather than departing from it); publication-lag/point-in-time handling was entirely unaddressed (fixed: as-of merge at t-1 business day, 5-day max-staleness); AGG/BND duration-mapping was an unpinned fork (fixed: nearest-tenor rule, ties round down). v2 corrected all of this with a level-based excess-carry construction (Treasury CMT minus 3-month bill; OAS directly for credit names) and kept TIP in a 10-name sleeve (not dropped to 9) specifically because `validation/canaries.py` hardcodes `min_names=10` internally and a 9-name sleeve would make the entire frozen canary suite silently return degenerate (falsely-passing) results -- a tension the first consult's own two recommendations (drop TIP / keep min_names=10 unchanged) left unresolved. **Second, narrower verification consult found one further correction needed, logged here as a pinned addendum rather than a v3 file (one paragraph, zero trial cost, no data pulled yet):** FRED's breakeven-inflation series is DEFINED as nominal CMT minus TIPS real CMT at the same tenor, so v2's "real yield + breakeven" construction for TIP is an exact arithmetic identity for the nominal CMT at that tenor -- no third FRED series is actually needed; TIP's carry is simply "nominal CMT at TIP's duration-matched tenor minus the 3-month bill," identical in formula to the other treasury-leg names. Disclosed consequence, accepted rather than engineered around: TIP's published effective duration (~6.5-7y) is close enough to IEF's (~7.5-8y) that both may map to the same nearest tenor, making their carry series identical on every date (a permanent rank tie, direction is power-reducing/conservative, not an integrity problem, but the "10 distinct names" framing should be read as "10 names, possibly fewer than 10 distinct carry values on some dates") | Verify against code, not narrative, applied to my own spec drafts, not just to code review: both design-reviewer passes caught real errors (an inverted analogy contradicting a logged decision; an algebraic identity I hadn't checked) rather than rubber-stamping architect output, exactly what the standing consult rule (D27) exists to do at increasing levels of construction detail as a spec gets more concrete | active |
| D36 | 2026-07-11 | ARCHITECT FINDING (logged, pending operator direction): EXP-004 v2's confirmatory test is STRUCTURALLY UNRUNNABLE as frozen -- FRED's ICE BofA OAS series (credit leg: LQD/HYG/JNK/EMB) have a rolling ~3-year trailing history window on FRED (live-verified: BAMLC0A0CM/BAMLH0A0HYM2/BAMLEMCBPIOAS all first-observe 2023-07-11, exactly "today minus 3y"; ALFRED vintage archive checked and confirmed equally truncated -- no point-in-time rescue exists). 2023-07-11 onward sits entirely inside the frozen 2022-01-01..2026-06-30 holdout, so the credit leg has ZERO usable dates on train/val -- the `min_names=10` confirmatory test (and even the `min_names=4` credit-only diagnostic) cannot score a single pre-holdout date. Discovered before any FRED data was pulled or any computation ran; zero trials consumed, ledger untouched. A design-reviewer consult (Fable) independently re-verified the finding, ruled out a Moody's-proxy rescue (BAA10Y is daily since 1986 but only substitutes for the IG name; HYG/JNK/EMB stay dead, and a <10-name sleeve makes `validation/canaries.py`'s hardcoded `min_names=10` default silently degenerate per D35's own logic), and confirmed the TIP/breakeven tenor question from D35 is already fully resolved (breakeven=nominal-minus-real means TIP's carry needs no breakeven series at all, just DGS7 nominal minus the 3mo bill -- not a new fork). Two live options identified for the operator: (C) defer Tier-2 carry, log the blocker, return to Phase 3 with S1+S2; (D) freeze a narrower v3 spec testing the 6-name TREASURY-ONLY leg (reverting D32's credit-leg broadening), with two defects that must be pinned ex-ante if chosen -- a <10-name sleeve's interaction with the frozen canary suite's hardcoded default, and the fact that the pinned tenor-mapping rule collapses 6 names to 4 distinct carry values every date (IEF/TIP tie at tenor=7, confirmed via published effective durations: SHY 1.9-2.0y->2, IEF 7.2y->7, TLT 15.20y->20, AGG 5.78y->5, BND 5.7-5.8y->5, TIP 6.41y->7) | FRED/ICE's 2022 licensing dispute permanently truncated historical vintages of ICE-sourced index series (public, well-documented, and independently reproduced live this session via both `get_series` and `get_series_as_of_date`/`get_series_all_releases`) -- not a bug in this implementation, not something waiting-it-out fixes (the window is rolling, so the gap never closes), and not discoverable before implementation-time verification (the spec itself deliberately left exact series IDs for this stage). Mission §7 pause: no construction-level fix exists within architect authority: every rescue either reverts D32's operator-approved credit-leg scope, substitutes a data family D28/D32 never approved, or shrinks the sleeve in a way that silently breaks frozen validation code -- each requires a logged operator call, not unilateral architect judgment | active |
| D37 | 2026-07-11 | OPERATOR APPROVAL (logged, in-conversation): given D36's blocker (FRED OAS credit-leg data structurally unavailable pre-holdout), do NOT fully defer Tier-2 carry -- instead build a yfinance-derived trailing-distribution-yield proxy for the credit leg (LQD/HYG/JNK/EMB), replacing OAS as the credit-leg data source, since the literature rates carry as the strongest candidate signal in the library and the operator does not want to lose it to a data-access problem alone. Explicitly NOT the literal Option 1 (full defer) or Option 2 (treasury-only, drop credit leg) from the D36 presentation -- a third path: keep all 10 names, substitute the credit-leg construction. Uses `yfinance` (already Phase-1-approved, no new data dependency) for ex-dividend distributions + price; treasury leg and duration-tenor-mapping methodology carry over from v2 unchanged. Operator's instruction: "do the construction properly to avoid the noise" -- architect responsible for designing the noise-mitigation (yield-window definition, PIT/ex-date handling, disclosed limitations) and running it past design-reviewer before freezing, per the standing D27 rule, since this is a materially new construction (a proxy, not OAS itself) that D32 did not literally approve | Tier-2 carry is the single highest-conviction candidate signal per the Phase-R literature review (D6); an untestable-for-data-reasons outcome is a worse loss to the project than the cost of building a careful proxy construction from data already inside the project's approved perimeter. The credit leg is what makes S5 defensible as genuine cross-asset carry rather than re-tested duration/term-premium (D32's own rationale) -- preserving it, properly, is worth the extra construction work over the free-but-blunt treasury-only fallback | active |
| D38 | 2026-07-11 | OPERATOR APPROVAL (logged, AskUserQuestion): for EXP-004 v3's holdout-period storage, use a SECOND, independent lockbox (`data/lockbox_carry/holdout.enc`, token `/workspace/OPERATOR_TOKEN_CARRY.txt`) rather than the D18-pattern "don't retain holdout data at all" alternative design-reviewer also offered. Covers both the derived S5_carry values AND the raw yfinance inputs (close+dividends for LQD/HYG/JNK/EMB) that a design-reviewer pass on the v3 draft found missing from an earlier version's storage section | Keeps holdout-period carry bit-reproducible now rather than depending on a future yfinance/FRED re-pull being stable, at the cost of a second token to secure off-pod and a future logged change to `validation/final_eval.py` (not needed until Phase 6) to consume it. Existing price-panel lockbox/token are untouched -- `build_lockbox` refuses to overwrite an existing enc/token pair by construction, so a second lockbox was the only way to keep full protection regardless of which storage option was chosen | active |
| D39 | 2026-07-11 | EXP-004-S5_carry EXECUTED against the frozen v3 spec: **graduates under the pre-registered rule** (mean_ic=0.100, t_nw=5.746, pct_years_positive=92.9% -- independently reproduced bit-identically by reviewer; the strongest result anywhere in the project, vs S1/S2's t=3.92/3.16 and S1_timing's marginal t=2.37), on the SECOND of two logged trials for this experiment (first run had a real units bug -- FRED percentage-point yields vs. a raw-decimal TTM yield subtracted without conversion -- caught by my own pre-audit via a rank-degeneracy check before either subagent ran; both runs logged, budget 8/250->10/250, not the spec's stale 9/250 estimate). **The graduating edge is NOT carry-timing information**: the spec's own required static/timing decomposition shows the static component alone at t=4.81 (raw-vs-static correlation 0.88) while the timing component is NEGATIVE and insignificant (t=-1.08, 46% years positive); leak-hunter's rank-stability analysis found week-over-week rank autocorrelation of 0.989, and a LITERALLY CONSTANT per-name signal (full-sample mean carry, fixed on every date) reproduces 98% of the raw signal's IC (t=5.33, correlation 0.966). The raw signal independently fails time_shift (leak-hunter re-ran it: base_t=5.75, lagged_t=5.08). Per design-reviewer's corrected framing: the hypothesis is CONFIRMED as pre-registered (not "falsified" -- no leg of the graduation rule was missed; declaring falsification post-hoc would itself be goalpost-moving), but the confirmed object is a static risk-premium tilt (long higher-carry/credit/duration names, short safer/short-duration names), not timed information -- this is D29's named "single largest known risk" (unconditional risky-beats-safe ordering) arriving concretely inside a counted confirmatory trial, holding through 2008(partial, the sleeve's best single year, IC+0.478)-2021, the exact ordering the 2022-start holdout is known to have inverted. S5_carry's construction is CLOSED (no defect found by either audit; re-tuning would hit the spec's locked-dial list and spend budget re-learning a known asset-class property, not new information) -- reopening requires explicit new operator authorization, not unilateral architect judgment. D34 evidence: S5_static correlates 0.55/0.56 with S1_static/S2_static -- partial, not full, overlap with the tilt those signals already carry. A secondary, project-level methodology finding (not S5-specific, not applied retroactively to S1/S2): the constant-signal control demonstrates the Phase-2 graduation rule has a measured blind spot for slow/persistent signals, flagged as a candidate future refinement for a separate operator decision. Open for the operator: does S5_static enter the Phase-3 candidate set as an explicitly-labeled static-tilt sleeve (correlation + inversion-risk disclosed), or is it shelved as D34-evidence-only with Phase 3 proceeding on S1+S2 alone? Records defects found by the audits (reviewer REQUEST-CHANGES, leak-hunter CONCERNS-FOUND, both logged to audit_log.jsonl) being corrected this round: the second lockbox still holds the buggy-construction payload (built during the first run, the corrected run hit the same one-shot FileExistsError guard the price panel uses -- needs operator-authorized delete+rebuild); the code's "see journal.md" citation is now resolved by a real entry; the spec is being committed alongside this round for git-anchored freeze-timing evidence (EXP-003 precedent) | Verify against code and data, not narrative, applied at the highest stakes yet in the project: the single strongest numeric result so far turned out, on the project's own required diagnostics, to be exactly the risk the project's strongest prior consult had already named as most dangerous -- the pre-registration + decomposition + canary + writer-does-not-verify machinery caught this cleanly rather than it reaching the operator framed as a win. Confirms the integrity architecture (frozen spec, mandatory decomposition, non-blocking-but-disclosed canaries, independent audits, standing design-reviewer consult) is doing exactly the job it was built for | active |
| D40 | 2026-07-11 | OPERATOR APPROVAL (logged, AskUserQuestion): S5_static enters the Phase-3 candidate set alongside S1/S2, explicitly labeled as a static risk-premium tilt (not carry-timing) -- the 0.55/0.56 correlation with S1_static/S2_static and the 2022-inversion risk (D29, D39) must be carried forward into every downstream report on it. Not shelved as D34-evidence-only (the alternative offered) | Correlation ~0.55/0.56 means real, partial diversification from S1/S2's existing static tilts, not a near-duplicate; Phase 3-5's own gates (PBO, CSCV, DSR, cost/regime stability) are the right place to let a disclosed, honestly-labeled static tilt be judged on its actual portfolio-level merit rather than pre-filtering it out at Phase 2 on the strength of the D29 concern alone | active |
